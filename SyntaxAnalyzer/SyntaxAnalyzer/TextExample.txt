begin
dim intOne, intTwo, index int
dim isFloat float
dim isBool bool

intOne = 101d;
let intTwo = 231d;
isFloat = 123E+12;

% your advertisement could be here %

for ( index ; index < intTwo; index = index + 1d) output (index);
if index <> intOne 
then output(index)
else input (index intOne) end_else;

do while intTwo <= 100d output (intOne or 251d) loop;
end
